{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Build C Program",
        "type": "shell",
        "command": "gcc",
        "args": [
          "-g",  // Include debugging information
          "-o", "${workspaceFolder}/output",  // Output binary
          "${workspaceFolder}/src/main.c"  // Input source file
        ],
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "problemMatcher": ["$gcc"],
        "detail": "Generated task for building the C program"
      }
    ]
  }
  